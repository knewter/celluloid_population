source :rubygems

gem 'celluloid'
gem 'pry'
gem 'rake'

group :development do
  gem 'yard'
  gem 'redcarpet' # Needed for README.md rendering in yardoc
end

group :test do
  gem 'mocha'
  gem 'minitest'
  gem 'minitest-reporters'
  gem 'guard-minitest'
end
